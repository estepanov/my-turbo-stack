{
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo build",
    "clean": "turbo clean",
    "dev": "dotenv -- turbo dev --no-cache --concurrency 15  --continue",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo lint",
    "start": "turbo start",
    "test": "turbo test"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^7.4.0",
    "dotenv-cli": "latest",
    "eslint": "^8.57.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "^1.13.0"
  },
  "packageManager": "pnpm@8.3.1",
  "engines": {
    "node": ">=20.0.0"
  }
}
